default:
{
    switches = [
        "--betterC",
        "--float-abi=soft",
        "--relocation-model=static",
        "--defaultlib=",
        "--preview=dip1000"
    ];
    post-switches = [
        "-I%%ldcbinarypath%%/../import",
    ];
    lib-dirs = [];
}
